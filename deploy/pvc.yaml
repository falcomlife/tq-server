apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tq
  namespace: self
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi      
  storageClassName: managed-nfs-storage

